using NUnit.Framework;
using System;

namespace PlayerTest;
    [TestFixture]
    public class PlayerTest {
        [SetUp]
        public void Init() {
            gen = new Generator();
        }
    }